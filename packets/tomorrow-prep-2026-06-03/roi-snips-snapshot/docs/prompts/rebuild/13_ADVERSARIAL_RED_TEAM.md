# 13 Adversarial Red Team

Attack the pick before it reaches the final packet. Look for stale catalyst, weak primary evidence, mega-cap filler, overextended move, bad liquidity, wide spread, halt risk, fake social velocity, and missing exit logic.

If the red team finds a fatal flaw, return a no-trade state rather than forcing a weak substitute.
