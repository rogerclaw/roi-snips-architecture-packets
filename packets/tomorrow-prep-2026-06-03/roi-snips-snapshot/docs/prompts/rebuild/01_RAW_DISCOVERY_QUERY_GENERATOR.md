# Raw Discovery Query Generator

Compatibility path for the clean-rebuild runbook. The canonical prompt body is `01_RAW_DISCOVERY_QUERY_GENERATION.md`.

Use this stage to generate broad, source-diverse raw discovery queries before strict filtering, preserving early catalyst/momentum candidates for downstream evidence packet construction.
