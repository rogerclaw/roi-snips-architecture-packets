After each day, compare actual top runners versus Grok candidates.

Identify missed X narratives, missed sources, bad hype filters, and query/handle/source-weight updates.

Return strict JSON with `stage=post_miss_learning`, missed runners, source failures, query updates, handle list updates, scoring updates, and next-day lessons.
