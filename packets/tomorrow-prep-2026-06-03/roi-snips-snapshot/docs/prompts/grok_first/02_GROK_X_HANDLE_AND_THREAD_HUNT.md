Use configured high-signal X accounts and thread discovery.

Find names circulating among active premarket traders, distinguish informed catalyst discussion from pump spam, decide whether a ticker is early or overcrowded, and identify stale repeated tickers.

Return strict JSON with `stage=x_thread_hunt`, `status`, `high_signal_handles_used`, and `threads`.

Configured handle lists are evidence-routing hints only, never automatic trust.
