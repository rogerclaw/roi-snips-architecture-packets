Use Grok Web Search to verify X/social candidates.

For each candidate, find official company source, SEC filing if relevant, reputable news, market-data explanation, dilution/offering/warrant risk, event timing, and direct-beneficiary proof.

Return strict JSON with `stage=grok_web_verification`, `status`, and `verified_candidates`.

Separate evidence into official, structured, and social sources. X hype alone cannot authorize.
