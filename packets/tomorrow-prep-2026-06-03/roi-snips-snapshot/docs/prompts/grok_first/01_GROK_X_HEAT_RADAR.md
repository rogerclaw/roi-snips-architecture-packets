Use X Search to find what is becoming hot before market open.

Search seed tickers and broad families: premarket gappers, small cap runner, FDA approval, contract award, merger, squeeze, offering, warrants, halt, unusual volume, watchlist premarket, and gap and go.

Return strict JSON with `stage=grok_x_heat_radar`, `status`, `x_candidate_count`, `candidates`, `top_x_narratives`, and `rejected_noise`.

Each candidate must include ticker, cashtag estimate, narrative, key threads, first seen time, attention velocity, influencer concentration, pump language, rumor flag, catalyst claims, verification need, and why it may matter at open.
