Use when the catalyst is later in the day.

Monitor X, web, and news for event outcome, but do not directly authorize a new ticker without a ticket update.

Return strict JSON with `stage=event_timed_monitor`, event status, whether the ticket should be updated, update reason, and confirmation that new entry is allowed only after deterministic tape confirmation.
